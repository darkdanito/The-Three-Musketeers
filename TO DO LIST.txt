BUGS:

	Fix the Nav Bar to our paper prototype
	There are times need to click twice to update current / destination\\
	Prevent carousel from automatically switch image (default is liddat)

TO DO LIST:

	Remake the LOGO in photoshop maybe ?
	Enlarge MRT image onclick()
	Highlight which input box the application currently is waiting (eg: current or destination)
	Remake reset button into clicking the respective button to reset instead
	Sort the MRT station by ascending order ?
	Allow user to search the list of mrt trains